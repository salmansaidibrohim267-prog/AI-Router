# Documentation Images

This directory reserves a home for documentation screenshots and diagrams.

> MANUAL ACTION REQUIRED — the following images must be captured from a
> running instance by a maintainer and committed here:
>
> - `screenshots/ready-health.png` — `curl /ready` and `/health` output
> - `screenshots/swagger.png` — Swagger UI at `/docs`
> - `screenshots/grafana-dashboard.png` — Grafana overview (monitoring profile)
> - `diagrams/architecture.png` — rendered architecture diagram
>
> Until images exist, the documentation references them by specification
> only (see `branding/social-preview.md` for the shared visual style).
